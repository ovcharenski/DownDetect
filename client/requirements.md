## Packages
recharts | For visualizing uptime graphs and response time charts
date-fns | For formatting dates and timestamps
framer-motion | For page transitions and component animations

## Notes
Dashboard auto-refreshes every 60 seconds
Status colors: Green (Healthy), Yellow (Degraded), Red (Unhealthy)
Backend provides `/api/apps/:internal_name/check` for manual triggering
